import type { APIRoute } from 'astro'; import { media } from '../../lib/cloudflare';
export const GET:APIRoute=async({params})=>{const key=decodeURIComponent(params.key||'');if(!key||key.startsWith('private-source/'))return new Response('Not found',{status:404});const object=await media().get(key);if(!object)return new Response('Not found',{status:404});const headers=new Headers();object.writeHttpMetadata(headers);headers.set('etag',object.httpEtag);headers.set('Cache-Control','public, max-age=31536000, immutable');return new Response(object.body,{headers});};
