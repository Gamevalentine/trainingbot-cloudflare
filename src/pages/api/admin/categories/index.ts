import type { APIRoute } from 'astro'; import { db } from '../../../../lib/cloudflare'; import { redirect } from '../../../../lib/admin'; import { slugify } from '../../../../lib/utils';
export const POST:APIRoute=async({request})=>{const f=await request.formData();const name=String(f.get('name')||'').trim();if(!name)return new Response('Thiếu tên',{status:400});await db().prepare('INSERT INTO categories (name,slug,description,icon,sort_order) VALUES (?,?,?,?,?)').bind(name,slugify(String(f.get('slug')||name)),String(f.get('description')||''),String(f.get('icon')||'folder'),Number(f.get('sort_order')||0)).run();return redirect('/admin/categories');};
